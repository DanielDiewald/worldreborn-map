import Link from "next/link";
import { notFound } from "next/navigation";
import { AdminShell } from "@/components/admin/admin-shell";
import { ConfirmAction } from "@/components/confirm-action";
import { EntityImageFrame } from "@/components/entity-image-frame";
import { EntityPicker } from "@/components/entity-picker";
import { FormRequiredLegend } from "@/components/form-ui";
import { ImageSourceInput } from "@/components/image-source-input";
import { SubmitButton } from "@/components/submit-button";
import { requireAdminSession } from "@/lib/auth/session";
import { getGroup, listGroupMemberships } from "@/lib/entities/groups";
import { getProjectEntityOption } from "@/lib/entity-search";
import { getProject } from "@/lib/projects";
import { archiveGroupAction, removeGroupMembershipAction, setGroupMembershipAction, updateGroupAction } from "../actions";
import { GroupMemberDialog } from "./group-member-dialog";

function validImage(value:unknown){return typeof value==="string"&&Boolean(value.trim())&&value!=="noimage"&&value!=="/noimg.jpg";}
function clean(value:unknown){const text=typeof value==="string"?value.trim():"";return text&&!['unknown','no notes yet'].includes(text.toLowerCase())?text:"";}
function personHref(projectId:number,personId:number,kind:string){return kind==="God"||kind==="Gottheit"?`/admin/projects/${projectId}/gods/${personId}`:`/admin/projects/${projectId}/npcs/${personId}`;}
function membershipStatusLabel(value:string){return value==="active"?"Aktiv":value==="former"?"Ehemalig":value==="pending"?"Ausstehend":value==="honorary"?"Ehrenmitglied":"Unbekannt";}

type Search=Promise<{personId?:string}>;
export default async function GroupDetailPage({params,searchParams}:{params:Promise<{projectId:string;groupId:string}>;searchParams:Search}){
  await requireAdminSession();const [raw,search]=await Promise.all([params,searchParams]);const projectId=Number.parseInt(raw.projectId,10);const groupId=Number.parseInt(raw.groupId,10);if(!Number.isSafeInteger(projectId)||!Number.isSafeInteger(groupId)||projectId<=0||groupId<=0)notFound();
  const requested=Number.parseInt(search.personId??"",10);const requestedPersonPromise=Number.isSafeInteger(requested)&&requested>0?getProjectEntityOption(projectId,"person",requested):Promise.resolve(null);
  const [project,group,members,requestedPerson]=await Promise.all([getProject(projectId),getGroup(projectId,groupId),listGroupMemberships(projectId,groupId),requestedPersonPromise]);if(!project||!group)notFound();const groupName=String(group.name);
  return <AdminShell projectId={projectId} projectName={project.name} section="groups" title={groupName} eyebrow={`${project.name} / Gruppen`}>
    <div className="breadcrumb"><Link href={`/admin/projects/${projectId}/groups`}>Gruppen</Link><span>/</span><strong>{groupName}</strong></div>
    <section className="entity-hero"><EntityImageFrame className="entity-avatar hero-avatar" src={validImage(group.image)?String(group.image):null} fallback={groupName.slice(0,1).toUpperCase()} alt={`${groupName} – Wappen`}/><div className="entity-hero-main"><span className="page-kicker">GRUPPE #{groupId} · {clean(group.group_type)||"Organisation"}</span><h1>{groupName}</h1>{clean(group.motto)?<p>{clean(group.motto)}</p>:null}<div className="hero-tags"><span className="soft-label">Hauptquartier: {String(group.location_name)}</span><span className="soft-label">{Number(group.estimated_member_count??group.members)} geschätzt</span><span className="soft-label">{members.length} bekannt</span></div></div></section>
    <form action={updateGroupAction.bind(null,projectId,groupId)} className="npc-detail-grid"><div className="stack detail-main"><FormRequiredLegend/><section className="panel-card edit-section"><div className="panel-heading"><div><span className="panel-kicker">GRUPPENPROFIL</span><h2>Organisation</h2></div><span className="record-id">gr_id #{groupId}</span></div><div className="field-grid two"><label>Name<input name="name" maxLength={100} defaultValue={groupName} required/></label><label>Typ <span className="muted">optional</span><input name="groupType" maxLength={80} defaultValue={group.group_type?String(group.group_type):""}/></label><EntityPicker projectId={projectId} name="locationId" types={["location"]} label="Hauptquartier" required allowClear={false} initialValue={String(group.loc_id)} initialLabel={String(group.location_name)} initialKind="Ort" placeholder="Ort suchen …"/><label>Motto <span className="muted">optional</span><input name="motto" maxLength={10000} defaultValue={clean(group.motto)}/></label><label>Sichtbarkeit<select name="visibilityMode" defaultValue={String(group.visibility_mode)}><option value="admin_only">Nur Admin</option><option value="all_players">Alle Spieler</option><option value="selected_players">Ausgewählte Spieler</option></select></label></div><ImageSourceInput current={validImage(group.image)?String(group.image):null} label="Bild / Wappen"/><label>Beschreibung / Notizen <span className="muted">optional</span><textarea className="large-textarea" name="notes" maxLength={100000} defaultValue={clean(group.notes)}/></label></section><div className="sticky-savebar"><div><strong>Gruppenprofil speichern</strong><span>Geschätzte Mitglieder werden durch Mitgliedschaftsänderungen nicht verändert.</span></div><SubmitButton className="primary" pendingLabel="Gruppe wird gespeichert …">Gruppenprofil speichern</SubmitButton></div></div><aside className="stack detail-side"><section className="panel-card profile-summary"><div className="panel-heading"><div><span className="panel-kicker">GRÖSSE</span><h2>Mitgliederzahlen</h2></div></div><dl className="summary-list"><div><dt>Geschätzt</dt><dd>{Number(group.estimated_member_count??group.members)}</dd></div><div><dt>Relational bekannt</dt><dd>{members.length}</dd></div><div><dt>Hauptquartier</dt><dd>{String(group.location_name)}</dd></div>{group.parent_group_name?<div><dt>Übergruppe</dt><dd>{String(group.parent_group_name)}</dd></div>:null}</dl></section><Link className="button primary" href={`/admin/projects/${projectId}/relationships?groupId=${groupId}&create=1`}>＋ Gruppen-Beziehung</Link><Link className="button" href={`/admin/projects/${projectId}/relationships?groupId=${groupId}`}>Gruppen-Beziehungen verwalten</Link></aside></form>

    <section className="panel-card"><div className="panel-heading"><div><span className="panel-kicker">MITGLIEDSCHAFTEN</span><h2>Bekannte Mitglieder</h2></div><div className="row"><strong>{members.length}</strong><GroupMemberDialog projectId={projectId} groupId={groupId} initialPerson={requestedPerson?{entityId:requestedPerson.entityId,name:requestedPerson.name,kind:requestedPerson.kind}:null} initialOpen={Boolean(requestedPerson)}/></div></div>
      {members.length===0?<div className="empty-state"><strong>Noch keine bekannten Mitglieder</strong><span>Über „Mitglied hinzufügen“ serverseitig nach Personen suchen.</span></div>:<div className="stack">{members.map((member)=><article className="panel-card nested-card" key={member.membership_id}><div className="row"><Link className="entity-cell" href={personHref(projectId,member.person_id,member.kind)}><EntityImageFrame className="entity-avatar" mode="thumbnail" src={member.image} fallback={member.name.slice(0,1).toUpperCase()} alt=""/><span><strong>{member.name}</strong><small>{member.kind}{member.is_leader?" · Leitung":""}</small></span></Link><span className="soft-label">{membershipStatusLabel(member.membership_status)}</span></div><form action={setGroupMembershipAction.bind(null,projectId,groupId,member.person_id)} className="field-grid two"><label>Rolle <span className="muted">optional</span><input name="role" maxLength={160} defaultValue={member.role??""}/></label><label>Rang <span className="muted">optional</span><input name="rank" maxLength={160} defaultValue={member.rank_label??""}/></label><label>Status<select name="status" defaultValue={member.membership_status}><option value="active">Aktiv</option><option value="former">Ehemalig</option><option value="pending">Ausstehend</option><option value="honorary">Ehrenmitglied</option><option value="unknown">Unbekannt</option></select></label><label><input name="isLeader" type="checkbox" defaultChecked={member.is_leader}/> Leitung</label><label>Beginn <span className="muted">optional</span><input name="startDisplay" maxLength={240} defaultValue={member.start_display??""}/></label><label>Ende <span className="muted">optional</span><input name="endDisplay" maxLength={240} defaultValue={member.end_display??""}/></label><label className="full-span">Notizen <span className="muted">optional</span><textarea name="membershipNotes" maxLength={100000} defaultValue={member.notes??""}/></label><div className="row"><SubmitButton pendingLabel="Mitgliedschaft wird gespeichert …">Mitgliedschaft speichern</SubmitButton></div></form><div className="row end"><ConfirmAction action={removeGroupMembershipAction.bind(null,projectId,groupId,member.person_id)} title={`${member.name} aus ${groupName} entfernen?`} description="Entfernt die kanonische Mitgliedschaft und bei Charakteren auch den synchronisierten Legacy-Shadow. Die Person selbst bleibt erhalten." triggerLabel="Mitgliedschaft entfernen…" confirmLabel="Mitgliedschaft entfernen"/></div></article>)}</div>}
    </section>
    <section className="danger-zone"><div><h2>Gruppe archivieren</h2><p>Keine harte Löschung; historische Daten bleiben erhalten.</p></div><ConfirmAction action={archiveGroupAction.bind(null,projectId,groupId)} title={`${groupName} archivieren?`} description="Die Gruppe verschwindet aus aktiven Listen. Historische und Legacy-Daten bleiben erhalten." triggerLabel="Archivieren…" confirmLabel="Gruppe archivieren" triggerClassName="button danger"/></section>
  </AdminShell>;
}
