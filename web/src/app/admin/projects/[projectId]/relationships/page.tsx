import Link from "next/link";
import { notFound } from "next/navigation";
import { AdminShell } from "@/components/admin/admin-shell";
import { ConfirmAction } from "@/components/confirm-action";
import { EntityImageFrame } from "@/components/entity-image-frame";
import { EntityPicker } from "@/components/entity-picker";
import { Pagination } from "@/components/pagination";
import { SubmitButton } from "@/components/submit-button";
import { requireAdminSession } from "@/lib/auth/session";
import { getProjectEntityOption } from "@/lib/entity-search";
import { listRelationshipTypes, listRelationshipsPaginated, type RelationshipListFilters, type ResolvedRelationship } from "@/lib/entities/relationships";
import { parsePagination } from "@/lib/pagination";
import { getProject } from "@/lib/projects";
import { deleteRelationshipAction, updateRelationshipAction } from "./actions";
import { RelationshipCreateDialog } from "./relationship-create-dialog";
import styles from "./relationships.module.css";

type RelType={relationship_type_id:number;code:string;label:string;inverse_label:string|null;category:string;directed:boolean};
type HouseRule="patrilineal"|"matrilineal"|"none";
type Search=Promise<{q?:string;type?:string;category?:string;status?:string;personId?:string;groupId?:string;kind?:string;page?:string;pageSize?:string;created?:string;updated?:string;create?:string;error?:string}>;
function positive(value?:string){const n=Number.parseInt(value??"",10);return Number.isSafeInteger(n)&&n>0?n:undefined;}
function entityHref(projectId:number,type:string,id:string,kind:string){if(type==="person")return kind==="Gottheit"||kind==="God"?`/admin/projects/${projectId}/gods/${id}`:`/admin/projects/${projectId}/npcs/${id}`;if(type==="group")return `/admin/projects/${projectId}/groups/${id}`;if(type==="location")return `/admin/projects/${projectId}/locations/${id}`;return undefined;}
function houseRule(value:unknown):HouseRule{return value==="matrilineal"||value==="none"||value==="patrilineal"?value:"patrilineal";}
function houseRuleLabel(value:HouseRule){return value==="matrilineal"?"matrilinear · Haus der Mutter":value==="none"?"keine automatische Hauszuordnung":"väterlich · Haus des Vaters";}
function visibilityLabel(value:string){return value==="all_players"?"Alle Spieler":value==="selected_players"?"Ausgewählte Spieler":"Nur Admin";}
function statusLabel(value:string){return value==="active"?"Aktiv":value==="ended"?"Beendet":value==="historical"?"Historisch":value==="unknown"?"Unbekannt":value;}
function categoryLabel(value:string){const labels:Record<string,string>={family:"Familie",romantic:"Romantik",social:"Sozial",political:"Politik",professional:"Beruf",religious:"Religion",other:"Sonstiges"};return labels[value]??value;}
function HouseRuleFields({value="patrilineal"}:{value?:HouseRule}){return <fieldset className="stack"><legend>Hausfolge bei Ehe</legend><label>In welches Haus werden gemeinsame Kinder geboren?<select name="houseDescentRule" defaultValue={value}><option value="patrilineal">Väterlich – Kinder gehören zum Haus des Vaters</option><option value="matrilineal">Matrilinear – Kinder gehören zum Haus der Mutter</option><option value="none">Keine automatische Hauszuordnung</option></select></label><p className="section-help">Wird nur für Ehebeziehungen zwischen zwei Personen gespeichert. „Alle Zweige“ nutzt diese Regel für die Haus-Blutlinie.</p></fieldset>;}
function Endpoint({projectId,type,id,name,kind,image}:{projectId:number;type:string;id:string;name:string;kind:string;image:string|null}){const href=entityHref(projectId,type,id,kind);const content=<div className={styles.endpoint}><EntityImageFrame className={styles.avatar} mode="thumbnail" src={image} fallback={name.slice(0,1).toUpperCase()} alt=""/><span><strong>{name}</strong><small>{kind}</small></span></div>;return href?<Link href={href}>{content}</Link>:content;}
function RelationRow({projectId,r,types,currentType}:{projectId:number;r:ResolvedRelationship;types:RelType[];currentType:number|undefined}){const legacy=Boolean(r.metadata.legacy_source);const descent=houseRule(r.metadata.house_descent_rule);return <div className={styles.relation}><Endpoint projectId={projectId} type={r.entity_a_type} id={r.entity_a_id} name={r.a_name} kind={r.a_kind} image={r.a_image}/><div className={styles.relationType}><strong>{r.type_label}</strong><small>{r.directed?"→ gerichtet":"↔ gegenseitig"}</small></div><Endpoint projectId={projectId} type={r.entity_b_type} id={r.entity_b_id} name={r.b_name} kind={r.b_kind} image={r.b_image}/><div className={styles.meta}><span>{statusLabel(r.status)}</span><span>{visibilityLabel(r.visibility_mode)}</span>{r.type_code==="spouse"?<span>Hausfolge: {houseRuleLabel(descent)}</span>:null}{r.start_display||r.end_display?<span>{r.start_display||"?"} – {r.end_display||"heute"}</span>:null}{legacy?<small>Legacy · schreibgeschützt</small>:<details><summary className="button ghost">Bearbeiten…</summary><form action={updateRelationshipAction.bind(null,projectId,Number(r.relationship_id))} className="stack" style={{minWidth:320}}><label>Beziehungstyp<select name="relationshipTypeId" required defaultValue={currentType}>{types.map((type)=><option key={type.relationship_type_id} value={type.relationship_type_id}>{categoryLabel(type.category)} · {type.label}</option>)}</select></label><label>Status<select name="status" defaultValue={r.status}><option value="active">Aktiv</option><option value="ended">Beendet</option><option value="historical">Historisch</option><option value="unknown">Unbekannt</option></select></label><label>Sichtbarkeit<select name="visibilityMode" defaultValue={r.visibility_mode}><option value="admin_only">Nur Admin</option><option value="all_players">Alle Spieler</option><option value="selected_players">Ausgewählte Spieler</option></select></label><div className="field-grid two"><label>Seit <span className="muted">optional</span><input name="startDisplay" maxLength={240} defaultValue={r.start_display??""}/></label><label>Bis <span className="muted">optional</span><input name="endDisplay" maxLength={240} defaultValue={r.end_display??""}/></label></div><HouseRuleFields value={r.type_code==="spouse"?descent:"patrilineal"}/><label>Öffentliche Beschreibung <span className="muted">optional</span><textarea name="publicDescription" maxLength={100000} defaultValue={r.public_description??""}/></label><label>Admin-Notizen <span className="muted">optional</span><textarea name="adminNotes" maxLength={100000} defaultValue={r.admin_notes??""}/></label><SubmitButton pendingLabel="Beziehung wird gespeichert …">Beziehungsdaten speichern</SubmitButton></form></details>}{!legacy?<ConfirmAction action={deleteRelationshipAction.bind(null,projectId,Number(r.relationship_id))} title="Beziehung entfernen?" description={`${r.a_name} ↔ ${r.b_name} wird entfernt und verschwindet auch aus den Stammbäumen. Die Entitäten selbst bleiben erhalten.`} triggerLabel="Entfernen…" confirmLabel="Beziehung entfernen"/>:null}</div></div>;}

export default async function RelationshipsPage({params,searchParams}:{params:Promise<{projectId:string}>;searchParams:Search}){
  await requireAdminSession();const [{projectId:rawProjectId},search]=await Promise.all([params,searchParams]);const projectId=Number.parseInt(rawProjectId,10);if(!Number.isSafeInteger(projectId)||projectId<=0)notFound();
  const relationshipTypeId=positive(search.type);const personId=positive(search.personId);const groupId=positive(search.groupId);const kind=["character","god","player"].includes(search.kind??"")?search.kind as RelationshipListFilters["entityKind"]:undefined;const pagination=parsePagination(search);const filters:RelationshipListFilters={query:search.q,relationshipTypeId,category:search.category||undefined,status:search.status||undefined,personId,groupId,entityKind:kind};
  const personOptionPromise=personId?getProjectEntityOption(projectId,"person",personId):Promise.resolve(null);const groupOptionPromise=groupId?getProjectEntityOption(projectId,"group",groupId):Promise.resolve(null);
  const [project,relationships,types,personOption,groupOption]=await Promise.all([getProject(projectId),listRelationshipsPaginated(projectId,filters,pagination),listRelationshipTypes(),personOptionPromise,groupOptionPromise]);if(!project)notFound();
  const categories=[...new Set(types.map((type)=>type.category))];const path=`/admin/projects/${projectId}/relationships`;const preselected=personOption??groupOption;const preselectedRef=personOption?`person:${personOption.entityId}`:groupOption?`group:${groupOption.entityId}`:"";const typeIdByCode=new Map(types.map((type)=>[type.code,type.relationship_type_id]));const hasFilters=Boolean(search.q||search.type||search.category||search.status||search.personId||search.groupId||search.kind);
  return <AdminShell projectId={projectId} projectName={project.name} section="relationships" eyebrow={`${project.name} / Wissen`} title="Beziehungen">
    <div className="page-heading compact-heading"><div><div className="breadcrumb"><Link href={`/admin/projects/${projectId}`}>{project.name}</Link><span>/</span><strong>Beziehungen</strong></div><h1>Beziehungszentrale</h1><p>Projektgebundene Beziehungen zwischen Personen, Gruppen und Orten. Personen werden intern einheitlich als <code>person + n_id</code> gespeichert. Ehe-Hausfolge steuert zusätzlich die Hauszweige im Stammbaum.</p></div><RelationshipCreateDialog projectId={projectId} types={types} initialOpen={search.create==="1"} preselected={preselected?{value:preselectedRef,label:preselected.name,kind:preselected.kind}:null}/></div>
    {search.created?<div className={styles.notice} role="status">Beziehung wurde erstellt und ist sofort in der Beziehungsansicht und den Stammbäumen verfügbar.</div>:null}{search.updated?<div className={styles.notice} role="status">Beziehung wurde aktualisiert.</div>:null}{search.error?<div className={`${styles.notice} ${styles.error}`} role="alert">{search.error}</div>:null}
    <section className="panel-card"><form className={styles.filterGrid} method="get"><input name="q" defaultValue={search.q??""} placeholder="Name oder Beziehung suchen …" aria-label="Beziehungen durchsuchen"/><select name="type" defaultValue={search.type??""} aria-label="Beziehungstyp"><option value="">Alle Typen</option>{types.map((type)=><option key={type.relationship_type_id} value={type.relationship_type_id}>{type.label}</option>)}</select><select name="category" defaultValue={search.category??""} aria-label="Kategorie"><option value="">Alle Kategorien</option>{categories.map((category)=><option key={category} value={category}>{categoryLabel(category)}</option>)}</select><select name="kind" defaultValue={search.kind??""} aria-label="Personenart"><option value="">Alle Personenarten</option><option value="character">NPC / Charakter</option><option value="player">Spielercharakter</option><option value="god">Gottheit</option></select><select name="status" defaultValue={search.status??""} aria-label="Status"><option value="">Alle Status</option><option value="active">Aktiv</option><option value="ended">Beendet</option><option value="historical">Historisch</option><option value="unknown">Unbekannt</option></select><EntityPicker projectId={projectId} name="personId" types={["person"]} placeholder="Person filtern …" initialValue={personOption?String(personOption.entityId):""} initialLabel={personOption?.name??""} initialKind={personOption?.kind??""}/><EntityPicker projectId={projectId} name="groupId" types={["group"]} placeholder="Gruppe filtern …" initialValue={groupOption?String(groupOption.entityId):""} initialLabel={groupOption?.name??""} initialKind="Gruppe"/><button type="submit">Filtern</button>{hasFilters?<Link className="button ghost" href={path}>Zurücksetzen</Link>:null}</form><div className="table-meta"><span><strong>{relationships.total}</strong> Beziehungen</span><span><Link href={`/admin/projects/${projectId}/family-trees`}>Stammbäume</Link> · <Link href={`/admin/projects/${projectId}/relationship-graph`}>Graph</Link></span></div>{relationships.items.length===0?<div className="empty-state large"><strong>Keine Beziehungen gefunden</strong><span>{hasFilters?"Filter ändern oder zurücksetzen.":"Lege die erste Beziehung an."}</span></div>:<div className={styles.relationList}>{relationships.items.map((r)=><RelationRow key={r.relationship_id} projectId={projectId} r={r} types={types} currentType={typeIdByCode.get(r.type_code)}/>)}</div>}<Pagination pathname={path} searchParams={{q:search.q,type:search.type,category:search.category,status:search.status,personId:search.personId,groupId:search.groupId,kind:search.kind}} page={relationships.page} pageSize={relationships.pageSize} total={relationships.total} totalPages={relationships.totalPages}/></section>
  </AdminShell>;
}
